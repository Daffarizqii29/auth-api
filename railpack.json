{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["npm install"]
    }
  },
  "deploy": {
    "startCommand": "npm run migrate up && node src/app.js"
  }
}