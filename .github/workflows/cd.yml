name: Continuous Deployment

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Placeholder deploy step
        run: echo "Continuous Deployment is temporarily disabled until server and SSH secrets are ready."